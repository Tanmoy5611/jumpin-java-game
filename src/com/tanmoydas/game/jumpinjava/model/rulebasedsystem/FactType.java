package com.tanmoydas.game.jumpinjava.model.rulebasedsystem;

// Defines the types of facts that can exist in the rule-based system
public enum FactType {
    CUP_AT,
    SAUCER_AT,
    TILE_EMPTY,
    CAN_MOVE
}